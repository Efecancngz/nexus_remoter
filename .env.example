# Copy to .env and fill in. The desktop agent reads this; it is never sent to the
# browser client (no VITE_ prefix, so Vite will not embed it in the bundle).
GEMINI_API_KEY="your-google-gemini-api-key"
